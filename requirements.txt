# CPU-only колесо torch (без CUDA ~2ГБ) — остальные пакеты берутся с PyPI
--extra-index-url https://download.pytorch.org/whl/cpu
Django==4.2.8
dj-database-url==2.1.0
djangorestframework==3.14.0
psycopg2-binary==2.9.9
python-decouple==3.8
Pillow==10.1.0
pytest==7.4.3
pytest-django==4.7.0
pytest-cov==4.1.0
factory-boy==3.3.0
faker==21.0.0
django-cors-headers==4.3.1
django-filter==23.5
gunicorn==21.2.0
whitenoise==6.6.0
cryptography==41.0.7
daphne==4.0.0
pandas==2.2.0
numpy==1.26.3
scikit-learn==1.4.2
torch==2.2.2
statsmodels==0.14.2
# Боевой ML-инференс LSTM без torch (Фаза 4, инкр. 5). 1.17.x совместим с numpy 1.26.
onnxruntime==1.17.3
# onnx нужен ТОЛЬКО для экспорта .pt→.onnx (torch.onnx.export), как torch — train-time.
# Боевой инференс его не требует (хватает onnxruntime).
onnx==1.16.0
